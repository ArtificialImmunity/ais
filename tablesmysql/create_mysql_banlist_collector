#Author Jordan Bruce

#MySQL commands to create tables to log banned IP's

CREATE TABLE bannedIPs ( id       INT          UNSIGNED NOT NULL AUTO_INCREMENT,
                         ip_src      INT      UNSIGNED NOT NULL,
                         ip_dst      INT      UNSIGNED NOT NULL,
                         reason     VARCHAR(255) NOT NULL,
                         timestamp 	   DATETIME NOT NULL,
                         PRIMARY KEY (id));